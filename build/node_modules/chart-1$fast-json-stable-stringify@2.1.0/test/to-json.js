'use strict';

Liferay.Loader.define('chart-1$fast-json-stable-stringify@2.1.0/test/to-json', ['module', 'exports', 'require', 'chart-1$tape', '..'], function (module, exports, require) {
    var define = undefined;
    var global = window;
    {
        var test = require('chart-1$tape');
        var stringify = require('..');

        test('toJSON function', function (t) {
            t.plan(1);

            t.equal(stringify({ one: 1, two: 2, toJSON: function () {
                    return { one: 1 };
                } }), '{"one":1}');
        });

        test('toJSON returns string', function (t) {
            t.plan(1);

            t.equal(stringify({ one: 1, two: 2, toJSON: function () {
                    return 'one';
                } }), '"one"');
        });

        test('toJSON returns array', function (t) {
            t.plan(1);

            t.equal(stringify({ one: 1, two: 2, toJSON: function () {
                    return ['one'];
                } }), '["one"]');
        });
    }
});
//# sourceMappingURL=to-json.js.map