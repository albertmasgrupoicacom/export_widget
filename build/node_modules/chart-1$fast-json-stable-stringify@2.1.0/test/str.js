'use strict';

Liferay.Loader.define('chart-1$fast-json-stable-stringify@2.1.0/test/str', ['module', 'exports', 'require', 'chart-1$tape', '..'], function (module, exports, require) {
    var define = undefined;
    var global = window;
    {
        var test = require('chart-1$tape');
        var stringify = require('..');

        test('simple object', function (t) {
            t.plan(1);

            t.equal(stringify({ c: 6, b: [4, 5], a: 3, z: null }), '{"a":3,"b":[4,5],"c":6,"z":null}');
        });

        test('object with undefined', function (t) {
            t.plan(1);
            var obj = { a: 3, z: undefined };
            t.equal(stringify(obj), '{"a":3}');
        });

        test('object with null', function (t) {
            t.plan(1);

            t.equal(stringify({ a: 3, z: null }), '{"a":3,"z":null}');
        });

        test('object with NaN and Infinity', function (t) {
            t.plan(1);
            var obj = { a: 3, b: NaN, c: Infinity };
            t.equal(stringify(obj), '{"a":3,"b":null,"c":null}');
        });

        test('array with undefined', function (t) {
            t.plan(1);
            var obj = [4, undefined, 6];
            t.equal(stringify(obj), '[4,null,6]');
        });

        test('object with empty string', function (t) {
            t.plan(1);

            t.equal(stringify({ a: 3, z: '' }), '{"a":3,"z":""}');
        });

        test('array with empty string', function (t) {
            t.plan(1);

            t.equal(stringify([4, '', 6]), '[4,"",6]');
        });
    }
});
//# sourceMappingURL=str.js.map