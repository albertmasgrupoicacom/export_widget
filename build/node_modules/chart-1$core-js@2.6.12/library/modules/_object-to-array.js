Liferay.Loader.define('chart-1$core-js@2.6.12/library/modules/_object-to-array', ['module', 'exports', 'require', './_descriptors', './_object-keys', './_to-iobject', './_object-pie'], function (module, exports, require) {
  var define = undefined;
  var global = window;
  {
    var DESCRIPTORS = require('./_descriptors');
    var getKeys = require('./_object-keys');
    var toIObject = require('./_to-iobject');
    var isEnum = require('./_object-pie').f;
    module.exports = function (isEntries) {
      return function (it) {
        var O = toIObject(it);
        var keys = getKeys(O);
        var length = keys.length;
        var i = 0;
        var result = [];
        var key;
        while (length > i) {
          key = keys[i++];
          if (!DESCRIPTORS || isEnum.call(O, key)) {
            result.push(isEntries ? [key, O[key]] : O[key]);
          }
        }
        return result;
      };
    };
  }
});
//# sourceMappingURL=_object-to-array.js.map