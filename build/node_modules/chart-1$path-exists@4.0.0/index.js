'use strict';

Liferay.Loader.define('chart-1$path-exists@4.0.0/index', ['module', 'exports', 'require', 'chart-1$fs', 'chart-1$util'], function (module, exports, require) {
	var define = undefined;
	var global = window;
	{
		const fs = require('chart-1$fs');
		const { promisify } = require('chart-1$util');

		const pAccess = promisify(fs.access);

		module.exports = async path => {
			try {
				await pAccess(path);
				return true;
			} catch (_) {
				return false;
			}
		};

		module.exports.sync = path => {
			try {
				fs.accessSync(path);
				return true;
			} catch (_) {
				return false;
			}
		};
	}
});
//# sourceMappingURL=index.js.map