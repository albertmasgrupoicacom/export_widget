'use strict';

Liferay.Loader.define('chart-1$pkg-dir@4.2.0/index', ['module', 'exports', 'require', 'chart-1$path', 'chart-1$find-up'], function (module, exports, require) {
	var define = undefined;
	var global = window;
	{
		const path = require('chart-1$path');
		const findUp = require('chart-1$find-up');

		const pkgDir = async cwd => {
			const filePath = await findUp('package.json', { cwd });
			return filePath && path.dirname(filePath);
		};

		module.exports = pkgDir;
		// TODO: Remove this for the next major release
		module.exports.default = pkgDir;

		module.exports.sync = cwd => {
			const filePath = findUp.sync('package.json', { cwd });
			return filePath && path.dirname(filePath);
		};
	}
});
//# sourceMappingURL=index.js.map