#!/usr/bin/env node
Liferay.Loader.define("chart-1$babylon@6.18.0/bin/babylon", ['module', 'exports', 'require', '..', 'chart-1$fs'], function (module, exports, require) {
  var define = undefined;
  var global = window;
  {
    /* eslint no-var: 0 */

    var babylon = require("..");
    var fs = require("chart-1$fs");

    var filename = process.argv[2];
    if (!filename) {
      console.error("no filename specified");
      process.exit(0);
    }

    var file = fs.readFileSync(filename, "utf8");
    var ast = babylon.parse(file);

    console.log(JSON.stringify(ast, null, "  "));
  }
});
//# sourceMappingURL=babylon.js.map